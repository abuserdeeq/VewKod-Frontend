# Copy this file to .env (or .env.local) and adjust as needed.
# Vite only exposes variables prefixed with VITE_ to client code.

# Base URL of the VewKod AI backend. Falls back to the production
# Render URL in src/api.js if this is not set.
VITE_API_BASE_URL=https://vewkod.onrender.com
